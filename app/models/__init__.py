from ..app import app, db

# nom = db.Table(
#     "nom",
#     db.Column('id', db.typevariable), db.ForeignKey('country.id'), primary_key=True),
#     db.Column('re_nom', db.type variable, db.ForeignKey('nom2.name'), primary_key=True)
# )
